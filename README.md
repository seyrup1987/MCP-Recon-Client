Clone the Repo

Install dependencies using UV pip install -r requirements.txt

Install Ollama Client from ollama.com and pull the LLM model you want to use with the client

If you have a Google Studio API Key, which you can obtain for free, update that to the '.env' file.

Run 'main'py' with UV run src/main.py
